const Logo = () => {
  return (
    <img
      src="/brand/logo.svg"
      alt="logo"
      className="mx-auto h-[40px] cursor-pointer transition-all duration-300 hover:scale-[1.01]"
    />
  );
};

export default Logo;
